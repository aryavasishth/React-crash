{
    "rewrites": [
      { "source": "/api/(.*)", "destination": "/api/jobs" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/$1" },
      { "src": "/api/(.*)", "methods": ["GET", "POST", "PUT", "DELETE"], "dest": "/api/jobs" }
    ]
  }
  